import { FieldErrorStateMatcher } from './field-error-state-matcher';

describe('FieldErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new FieldErrorStateMatcher()).toBeTruthy();
  });
});
